import PackageDescription

let package = Package(
    name: "PINRemoteImage",
    products: [
        .library(
            name: "PINRemoteImage",
            targets: ["PINRemoteImage"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "PINRemoteImage",
            dependencies: []),
    ]
)
